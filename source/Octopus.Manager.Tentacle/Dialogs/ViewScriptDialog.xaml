<Window 
    x:Class="Octopus.Manager.Tentacle.Dialogs.ViewScriptDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    mc:Ignorable="d" 
    d:DesignHeight="300" d:DesignWidth="300"
    Width="800"
    Height="300"
    >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBox Name="ScriptTextBox" Style="{x:Null}" Background="White" Foreground="#333" FontFamily="Consolas" TextWrapping="NoWrap" Padding="10" FontSize="11pt" IsReadOnly="True" HorizontalScrollBarVisibility="Visible" VerticalScrollBarVisibility="Visible" VerticalAlignment="Stretch" />
        <Button Grid.Row="1" Style="{DynamicResource CopyToClipboardButton}" HorizontalAlignment="Right" Margin="2" Click="CopyToClipboardButton"/>
    </Grid>
</Window>
