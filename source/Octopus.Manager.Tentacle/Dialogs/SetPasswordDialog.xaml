<Window 
    x:Class="Octopus.Manager.Tentacle.Dialogs.SetPasswordDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    mc:Ignorable="d" 
    Width="400" Height="130" Title="Set Password"
    WindowStartupLocation="CenterOwner"
    ResizeMode="NoResize"
    Name="_this"
    DataContext="{Binding ElementName=_this}"
    Background="#fff"
    FontSize="10pt"
    >
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="30" />
        </Grid.RowDefinitions>
        
        <StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <TextBlock Text="Password:" VerticalAlignment="Center" Width="100" />
                <PasswordBox Name="password" Padding="4,3,4,3" Margin="20,0,0,0" Width="246" FontFamily="Consolas" FontSize="11pt" FontWeight="Bold" />
            </StackPanel>
        </StackPanel>
        
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="SAVE" Width="100" IsDefault="True" Click="SaveClicked" Style="{StaticResource Button.Success}" />
            <Button Content="CANCEL" Width="100" Margin="10,0,0,0" IsCancel="True" />
        </StackPanel>
    </Grid>
</Window>
